# tricor

Construct disordered-to-ordered atomic supercells from 3 body distributions.

## Development setup

```bash
uv sync
```

This initializes the project environment and installs the current starter
dependencies:

- `numpy`
- `matplotlib`
- `ase`
